<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chat</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
  <link href="/css/styles.css" rel="stylesheet" type="text/css">
</head>
<body>
  <div class="container">
    <div class="navBar">
      <h1>Hola <%= name %>
      </h1>
      <a href="/logout"> Deslogearse </a>
    </div>
    <h2>Chat</h2>

    <% if(admin) {%>

      <h3>Responder a un usuario</h3>
      <form action="#" id="systemMessageForm">
        <div class="formInput">
          <label for="selectEmail">Selecciona un usuario para responder</label>
          <select name="selectEmail" id="selectEmail"></select>
        </div>
        <div class="formInput">
          <label for="systemMessage"> Escribe un mensaje</label>
          <input type="text" name="systemMessage" id="systemMessage" required>
        </div>
        <button type="submit" class="btnSubmit"> Enviar respuesta </button>
      </form>

      <% } else { %>

        <h3 class="my-5">Envía un mensaje al servidor! </h3>
        <form action="#" id="userMessageForm">
          <div class="formInput">
            <label for="userMessage">Escribe un mensaje</label>
            <input type="text" name="userMessage" id="userMessage">
          </div>
          <button type="submit" class="btnSubmit"> Enviar mensaje</button>
        </form>
        <% } %>
          <!-- <% if ( !admin ) { %>
            <div class="links my-5">
              <a href="/chat/<%=email%>"> Ver solo mis mensajes </a>
            </div>
            <% } %> -->
          <div>
            <ul id="messagesContainer"></ul>
          </div>
          <div class="links">
            <a href="/products"> Ir a productos </a>
          </div>
  </div>
  <div style="display:none;" id="email">
    <%=email%>
  </div>
  <script src="/socket.io/socket.io.js"></script>
  <script defer src="main.js"></script>
</body>
</html>